/*Debug and fix the below code so that it can print 0 as an output*/ 

#include <stdio.h>
int main()
{
 int x,y;
 printf("enter the value of x & y:");
 scanf("%d%d",&x ,&y);
  int z = 8 = x = y ;
  printf("%d",z);
  return 0;
}
